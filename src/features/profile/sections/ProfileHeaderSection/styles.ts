import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: "center",
    },

    infoContainer: {
        marginTop: 16,
        alignItems: "center",
    },

    email: {
        marginTop: 4,
    },

    editButton: {
        marginTop: 16,
    },
});
