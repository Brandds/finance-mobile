import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 20,
    borderRadius: 24,
  },

  title: {
    marginBottom: 12,
  },

  value: {
    marginBottom: 16,
  },

  footer: {
    flexDirection: "row",
    alignItems: "center",
  },

  info: {
    marginLeft: 8,
  },
});